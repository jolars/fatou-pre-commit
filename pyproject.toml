[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fatou-pre-commit"
version = "0.14.0"
description = "pre-commit hook wrapper for fatou"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = ["fatou==0.14.0"]

[tool.setuptools]
py-modules = []
